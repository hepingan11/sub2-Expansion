interface:
  display_name: "Sub2API Admin"
  short_description: "Manage Sub2API, check-ins, and invitation rewards"
  default_prompt: "Use $sub2api-admin to request a private invitation binding URL and inspect invitation rewards."
